# Agent Guidance

Before working on NestJS CRUD Automator implementation or documentation, read the project skill at `.cursor/skills/crud-automator/SKILL.md`.

Use that skill as the canonical repository guidance for Cursor, Codex-style agents, and any other agent runner that reads `AGENTS.md`.

Also follow `.cursor/rules/file-organization.mdc` for file organization, one entity per file, recursive prefix grouping, and barrel exports.

When docs, examples, or prior agent guidance conflict with source, treat `src/interface/**`, `src/type/**`, exported barrels, and tests as the contract.
